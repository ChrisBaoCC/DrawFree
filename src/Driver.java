/**
 * <code>Driver</code> class. Runs the program.
 */
public class Driver {
	/**
	 * Main method. Entry point for the app to run.
	 * @param args Arguments from the command line. Should be empty but doesn't matter.
	 */
	public static void main(String[] args) {
		Frame f = new Frame();
	}
}
